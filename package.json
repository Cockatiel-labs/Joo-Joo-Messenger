{
  "name": "cockatiel-messenger",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "lint": "biome check .",
    "format": "biome format --write .",
    "dev:web": "bun --filter @cockatiel/web dev",
    "dev:api": "bun --filter @cockatiel/api dev",
    "build:web": "bun --filter @cockatiel/web build",
    "build:api": "bun --filter @cockatiel/api build"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.16"
  }
}
